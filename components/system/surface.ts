/**
 * The workstation card shell — every queue surface (metric ribbon, data grid,
 * empty state, mobile row card) sits on exactly this. One constant so a change
 * to the elevation or the border tone moves all of them together.
 */
export const SURFACE_CARD =
  "rounded-xl border border-slate-200/80 bg-white shadow-xs overflow-hidden";
